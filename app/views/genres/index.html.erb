<div class="container">
	<h3>ALL GENRES</h3>

  <div class="row cards" style="margin:30px 0;">
    <% @genres.order(:name).each do |genre| %>
        <%= render partial: 'genre_card', locals: {genre: genre, class_name: ""} %>
    <% end %>
  </div>
</div>
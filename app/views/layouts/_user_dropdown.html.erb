<div class="btn-group user-dropdown">
  <button type="button" id="user_dropdown_button" data-toggle="dropdown">
    <%= cl_image_tag(current_user.picture, :crop => :fill, :alt => "Avatar", class: "rounded-circle img-circle", style:"max-width:25px;") if current_user.picture? %>
    <%= cl_image_tag("static/blank_user.png", :crop => :fill, :alt => "User Image", class: "rounded-circle img-circle", style:"max-width:25px;") unless current_user.picture? %> 
  </button>
  <ul class="dropdown-menu dropdown-menu-right" role="menu">
    <li>
      <%= link_to "Profile", user_path(current_user) %>
    </li>
    <li>
      <%= link_to "Settings", '/users/edit' %>
    </li>
    <li>
      <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
    </li>
  </ul>
</div>

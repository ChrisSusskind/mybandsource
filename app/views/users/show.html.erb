<div class="container">
	
	<div class="row">
		<div class="col-sm-6">
			<img src="<%= Faker::LoremPixel.image("300x300", true, 'people') %>" class="rounded">
			<h3><%= @user.name.presence || "Lone Wanderer" %></h3>
			<p class="lead">Email: <a href="mailto:<%= @user.email %>"><%= @user.email %></a></p>

			<%= link_to 'Edit', edit_user_path(@user) if current_user == @user %>
		</div>

		<div class="col-sm-6">
			<h3>Favorite Bands</h3>

		</div>
	</div>

	

</div>

